============
Installation
============

At the command line::

    $ pip install wikiparsercli

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiparsercli
    $ pip install wikiparsercli

Or, if you are using pipenv::

    $ pipenv install wikiparsercli

Or, if you are using pipx::

    $ pipx install wikiparsercli
